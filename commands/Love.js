const Discord = require("discord.js");

module.exports.run = async (bot, message, args) => {
    return message.channel.send("I Love you Darkness, don't give up and I won't give up, we are in this together. :heart:! **Note that this message never changes it is for someone I care about so it will stay**");
}

module.exports.help = {
  name: "love2"
};
